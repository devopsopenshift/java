# File: template.yml
#trigger:
#- azure-pipelines-2

#pool:
#  vmImage: 'ubuntu-latest'

parameters:
- name: APPLICATION # the name of the parameter is buildSteps
  type: string # data type is StepList
  default: JDPrism # default value of buildSteps

#variables:
#- name: COUD_FORMATION_PKG_PATH
#  value: 'aws-cloudformation/'
#  readonly: true

steps:
  - script: |
      echo ${{ variables.COUD_FORMATION_PKG_PATH }} # outputs initialValue
    displayName: Display COUD_FORMATION_PKG_PATH
  - bash: echo ${{ parameters.APPLICATION }}
    displayName: Display APPLICATION
  
  - task: PythonScript@0
    inputs:
      scriptSource: 'inline'
      script: 'print ''hello'''
      failOnStderr: true
